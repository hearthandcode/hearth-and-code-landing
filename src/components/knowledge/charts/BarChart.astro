---
/**
 * BarChart - D3 bar chart (re-export of HorizontalBar with rotation)
 * Knowledge chart: k-chart-bar
 */
import * as d3 from 'd3';
interface Datum { label: string; value: number; color?: string; }
interface Props {
  data: Datum[];
  height?: number;
  showAxis?: boolean;
  showValues?: boolean;
  class?: string;
}
const { data, height = 240, showAxis = true, showValues = true, class: className = '' } = Astro.props;
const chartId = `kc-bar-${Math.random().toString(36).slice(2, 9)}`;
const width = 320;
const margin = { top: 12, right: 12, bottom: showAxis ? 28 : 4, left: showAxis ? 36 : 4 };
const innerW = width - margin.left - margin.right;
const innerH = height - margin.top - margin.bottom;
const max = Math.max(...data.map((d) => d.value), 1);
---
<div class:list={['kc-d3-bar', className]} data-chart-id={chartId} data-props={JSON.stringify({ data: data, height: height, showAxis: showAxis, showValues: showValues })}>
  <svg viewBox={`0 0 ${width} ${height}`} xmlns="http://www.w3.org/2000/svg" class="kc-d3-bar__svg"></svg>
</div>


  <script type="application/json" id={`props-${chartId}`} set:html={JSON.stringify({ data: data, height: height, showAxis: showAxis, showValues: showValues })}></script>
<style>
  .kc-d3-bar { background: var(--color-surface-recessed); border-radius: var(--radius-sm); padding: var(--space-2); }
  .kc-d3-bar__svg { width: 100%; height: auto; display: block; }
</style>
