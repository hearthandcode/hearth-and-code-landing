/**
 * EvidenceStrength (React port)
 *
 * Auto-generated from: k-evidence-strength
 * Source: src/components/knowledge/atoms/EvidenceStrength.astro
 *
 * NOTE: Skeleton port. Visual fidelity depends on host framework CSS.
 */

export interface EvidenceStrengthProps {
  tier: string;
  showLabel: boolean;
}

export function EvidenceStrength({
  tier,
  showLabel = true,
}: EvidenceStrengthProps) {
  return (
    <div className="kc-k-evidence-strength kc-k-evidence-strength--placeholder">
      <span>EvidenceStrength (React port)</span>
      {/* TODO: port rendering logic from src/components/knowledge/atoms/EvidenceStrength.astro */}
    </div>
  );
}
